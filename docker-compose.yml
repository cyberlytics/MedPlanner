version: "3.1"
   
services:
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: j8Rqe.fu

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 4222:4222
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: 0Y7jrmp?
  #web:
   # build: .
   # command: python manage.py runserver 0.0.0.0:4221
    #volumes:
    #  - .:/django
    #ports:
    #  - "4221:4221"
   # depends_on:
    #  - mongo
